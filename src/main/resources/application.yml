#Spring Datasource
server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ksdb
    username: kaizen
    password: KZ@db!123456789
    driver-class-name: org.postgresql.Driver

  #JPA Hibernate
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        format_sql: true
